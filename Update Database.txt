CREATE TABLE EXPERIENCE(
  ID_EXPERIENCE NUMBER PRIMARY KEY,
  ID_PROJET NUMBER DEFAULT 0,
  CONSTRAINT fk_experince_projet FOREIGN KEY(ID_PROJET) REFERENCES PROJET(ID_PROJET),
  DESCRIPTION VARCHAR2(2000) DEFAULT ''

);
ALTER TABLE EXPERIENCE ADD(NOM VARCHAR2(50) DEFAULT '')

ALTER TABLE PROJET ADD(DESCRIPTION_PROJET VARCHAR2(2000) DEFAULT '');


CREATE TABLE HISTORIQUE_STOCK_VACCIN(
ID_VACCIN NUMBER,
DATE_CHANGEMENT NUMBER,
ANCIEN_STOCK NUMBER,
NOUVEAU_STOCK NUMBER,
CONSTRAINT cpk_vaccin_date PRIMARY KEY(ID_VACCIN,DATE_CHANGEMENT),
CONSTRAINT fk_vaccin_ FOREIGN KEY(ID_VACCIN) REFERENCES VACCIN(ID_VACCIN)
);